/**
 * Settings helpers for the Pomodoro timer.
 *
 * @module     local_pomodoro/settings
 * @copyright  2026 José Carlos
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("local_pomodoro/settings",["local_pomodoro/constants","local_pomodoro/default_settings","local_pomodoro/storage"],(function(Constants,Defaults,Storage){const clampInteger=function(value,key){const limits=Defaults.limits[key],parsed=parseInt(value,10),safeValue=Number.isFinite(parsed)?parsed:Defaults.settings[key];return Math.min(Math.max(safeValue,limits.min),limits.max)},clampDurationSeconds=function(value){const limits=Defaults.limits.durationSeconds,parsed=parseInt(value,10),safeValue=Number.isFinite(parsed)?parsed:limits.min,clamped=Math.min(Math.max(safeValue,limits.min),limits.max);return Math.round(clamped/limits.step)*limits.step},setDuration=function(settings,prefix,totalSeconds){const safeSeconds=clampDurationSeconds(totalSeconds),secondKey=prefix+"Seconds";settings[prefix+"Minutes"]=Math.floor(safeSeconds/60),settings[secondKey]=safeSeconds%60},getDurationByPrefix=function(settings,prefix){return 60*settings[prefix+"Minutes"]+settings[prefix+"Seconds"]},durationFieldPrefixes={focusDurationSeconds:"focus",shortBreakDurationSeconds:"shortBreak",longBreakDurationSeconds:"longBreak"},modePrefixes={short_break:"shortBreak",long_break:"longBreak"},formatDurationLabel=function(totalSeconds){const seconds=clampDurationSeconds(totalSeconds),minutes=Math.floor(seconds/60),remainingSeconds=seconds%60;return 0===minutes?remainingSeconds+"s":0===remainingSeconds?minutes+"min":minutes+"min "+remainingSeconds+"s"},normaliseSettings=function(settings){const merged=Object.assign({},Defaults.settings,settings||{}),normalised={focusMinutes:clampInteger(merged.focusMinutes,"focusMinutes"),focusSeconds:clampInteger(merged.focusSeconds,"focusSeconds"),shortBreakMinutes:clampInteger(merged.shortBreakMinutes,"shortBreakMinutes"),shortBreakSeconds:clampInteger(merged.shortBreakSeconds,"shortBreakSeconds"),longBreakMinutes:clampInteger(merged.longBreakMinutes,"longBreakMinutes"),longBreakSeconds:clampInteger(merged.longBreakSeconds,"longBreakSeconds"),cyclesBeforeLongBreak:clampInteger(merged.cyclesBeforeLongBreak,"cyclesBeforeLongBreak")};return setDuration(normalised,"focus",getDurationByPrefix(normalised,"focus")),setDuration(normalised,"shortBreak",getDurationByPrefix(normalised,"shortBreak")),setDuration(normalised,"longBreak",getDurationByPrefix(normalised,"longBreak")),normalised},getSettings=function(){return normaliseSettings(Storage.readJson(Constants.keys.settings,Defaults.settings))};return{formatDurationLabel:formatDurationLabel,getDurationSeconds:function(mode,settings){return getDurationByPrefix(settings,modePrefixes[mode]||"focus")},getSettings:getSettings,normaliseSettings:normaliseSettings,readSettingsFrom:function(root,selector){const next=Object.assign({},getSettings());return root.querySelectorAll(selector).forEach((function(input){const key=input.dataset.pomodoroSettingField||input.dataset.pomodoroPanelField,prefix=durationFieldPrefixes[key];prefix?setDuration(next,prefix,input.value):next[key]=input.value})),normaliseSettings(next)},saveSettings:function(settings){const normalised=normaliseSettings(settings);return Storage.writeJson(Constants.keys.settings,normalised),normalised},writeSettingsTo:function(root,selector,settings){root.querySelectorAll(selector).forEach((function(input){if(document.activeElement===input)return;const key=input.dataset.pomodoroSettingField||input.dataset.pomodoroPanelField,prefix=durationFieldPrefixes[key];input.value=prefix?getDurationByPrefix(settings,prefix):settings[key];const output=input.parentNode.querySelector("[data-pomodoro-range-output]");output&&-1!==key.indexOf("DurationSeconds")?output.textContent=formatDurationLabel(input.value):output&&(output.textContent=input.value)}))}}}));

//# sourceMappingURL=settings.min.js.map