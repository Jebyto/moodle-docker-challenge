/**
 * Browser tab leadership for Pomodoro notifications.
 *
 * @module     local_pomodoro/tabs
 * @copyright  2026 José Carlos
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("local_pomodoro/tabs",["local_pomodoro/constants","local_pomodoro/storage"],(function(Constants,Storage){let tabId=null,leaderIntervalId=null;const getTabId=function(){return tabId||(tabId=function(){try{const existing=window.sessionStorage.getItem(Constants.keys.tabId);if(existing)return existing;const next="tab-"+Date.now()+"-"+Math.random().toString(36).slice(2);return window.sessionStorage.setItem(Constants.keys.tabId,next),next}catch(error){return"tab-"+Date.now()+"-"+Math.random().toString(36).slice(2)}}()),tabId},claimLeadership=function(){const now=Date.now(),current=Storage.readJson(Constants.keys.activeTab,null),currentTabId=getTabId();return!current||current.tabId===currentTabId||!current.heartbeat||now-current.heartbeat>Constants.leaderTtlMs?(Storage.writeJson(Constants.keys.activeTab,{tabId:currentTabId,heartbeat:now}),!0):current.tabId===currentTabId};return{claimLeadership:claimLeadership,getTabId:getTabId,startLeaderHeartbeat:function(){leaderIntervalId||(getTabId(),leaderIntervalId=window.setInterval(claimLeadership,2e3),claimLeadership())}}}));

//# sourceMappingURL=tabs.min.js.map